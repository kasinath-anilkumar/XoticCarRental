/** Shared bounds keep the calculator, URL parser and public APIs in agreement. */
export const MAX_TRIP_STOPS = 12;
/** Passenger stops plus the vehicle's garage at each end. */
export const MAX_ROUTE_POINTS = MAX_TRIP_STOPS + 2;
export const MAX_TRIP_DAYS = 30;
